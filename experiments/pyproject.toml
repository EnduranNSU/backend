[project]
name = "experiments"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Ivan Shebanov", email = "vanamokanov8@gmail.com"},
]
dependencies = ["ipykernel>=7.2.0", "jupyter>=1.1.1", "cv-service @ file:///${PROJECT_ROOT}/../cv_service", "open3d>=0.19.0", "imageio>=2.37.3"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
